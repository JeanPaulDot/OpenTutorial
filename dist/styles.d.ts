/**
 * The stylesheet, as a string.
 *
 * This is the single source of truth: `dist/styles.css` is generated from it at
 * build time, and shadow-isolated layers inject it directly. Keeping one copy
 * means the two can never drift.
 */
export declare const CSS: string;
export default CSS;
